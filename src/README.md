# FFS Code Organization

- `app` - Main FFS application code
- `deploy` - Deployment helpers, including auto-install and special release configurations
- `lib` - Library code that can be individually tested
- `scripts` - Developer scripts for building, bundling, generating assets, and other tooling tasks
- `test` - Top level automated tests
